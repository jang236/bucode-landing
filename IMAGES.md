# 세일즈 이미지 제작 사양

부코드님이 이미지를 만들어 `img/sales/` 에 넣으면 해당 자리에 자동으로 표시됩니다.
**아직 없는 이미지는 슬롯이 통째로 숨겨지고 기존 HTML 텍스트가 그대로 보입니다.** 한 장씩 채워 넣어도 페이지가 깨지지 않습니다.

## 자리 확인하는 법

```
https://jang236.github.io/bucode-landing/?slots=1
```

주소 끝에 `?slots=1` 을 붙이면 아직 안 채워진 슬롯이 **빨간 점선 박스**로 표시되고, 그 안에 파일명과 용도가 적혀 있습니다. 실제 방문자에게는 안 보입니다.

## 파일 규칙

| 항목 | 값 |
|---|---|
| 위치 | `img/sales/` |
| 모바일 | `mo_01.jpg` ~ `mo_10.jpg` · 가로 **1080px** |
| 데스크톱 | `pc_01.jpg` ~ `pc_10.jpg` · 가로 **1720px** |
| 확장자 | `.jpg` 고정 (PNG로 만드셨으면 jpg로 저장) |
| 세로 | 자유 |

- **모바일만 만드셔도 됩니다.** `pc_XX.jpg` 가 없으면 데스크톱에서도 모바일 이미지를 씁니다. 반대로 `mo_XX.jpg` 는 반드시 있어야 합니다.
- 이미지 안 글자는 **1080px 기준 최소 32px 이상**으로 키워주세요. 주 독자가 40~50대입니다.
- 좌우 가장자리 40px 안쪽으로 글자를 배치해주세요. 기기에 따라 잘릴 수 있습니다.
- 장당 400KB 이하 권장. 넘으면 모바일에서 느려집니다.

## 슬롯 10개

| 파일 | 위치 | 담을 내용 |
|---|---|---|
| `01` | 블록2 · 표면 문제 | 세 가지 문제 시각화 — 좋은 회사인지 모르고 삼 / 왜 떨어졌는지 모름 / 만회할 시간 없음 |
| `02` | 블록3 · 이중 면죄부 | 유튜버·증권사·AI 가 각각 왜 실패하는지 비교 |
| `03` | 블록4-하나 | 네이버 증권 재무 지표 복사 → AI 다섯 항목 평가 결과 화면 |
| `04` | 블록4-둘 | 업종 평균 비교(✗) vs 그 회사의 과거 PER 비교(○) |
| `05` | 블록4-셋 | ✅ **완료** — 알테오젠 전자공시 탭 (무상증자·권리락·거래정지) |
| `06` | 블록6 · 근본 진단 | 다른 도구는 "좋은 회사인가"까지만, 적정가 칸은 비어 있음 |
| `07` | 블록7 · 상품 | 네 단계 흐름도 (재무 필터링 → AI 재무분석기 → 적정가 판정 → 공시 검증) |
| `08` | 블록7 · 상품 | 실제 도구 사용 화면 — 종목명 입력하면 결과가 나오는 장면 |
| `09` | 블록8 · 가격 | ✅ **완료** — 외주 플랫폼 24만원 견적 (카피도 24만원/96만원으로 수정됨) |
| `10` | 블록9 · 후기 | ✅ **완료** — 여섯시십분·지웅군·방울토마토마토 (주식농부는 수익 서술 때문에 제외) |

## 주의

- 원본 캡처는 `img/sales/raw/` 에 보관 — **gitignore 처리돼 배포되지 않습니다** (주식농부 후기 원본 등 미사용분 포함).
- **`10` 후기: 수익·손실 금액이나 특정 종목명이 찍힌 후기는 쓰지 않습니다** (확정본 삭제 목록 사항). 주식농부 후기가 이 이유로 제외됐습니다.
- `03` 실제 화면 캡처에는 **본인 계좌 잔고·보유 종목이 찍히지 않도록** 확인해주세요.
- 삭제된 축(속도)의 표현은 넣지 마세요 — "2시간→10초", "10초 분석", "33개 지표", "매수근거 5가지"는 확정본에서 뺀 문구입니다.

## 히어로 배경

히어로는 슬롯이 아니라 CSS 배경입니다. 바꾸시려면 `img/hero-night.jpg` 를 같은 이름으로 덮어쓰면 됩니다 (가로 1600px 이상, 어두운 톤 권장 — 위에 흰 글자가 올라갑니다).
